# evals_public/sample_results/

This directory contains sample evaluation results from public eval runs.

Full results are in [`examples/eval_runs/`](../examples/eval_runs/).

To generate your own eval results using the stub runner:

```bash
python -m evals_public.run_public_evals
```
